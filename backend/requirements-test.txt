# PaperBuddy ERP — Testing Dependencies
# Install with: pip install -r requirements-test.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.27.0
anyio[trio]>=4.0.0
aiosqlite>=0.19.0          # In-memory SQLite for test DB
asgi-lifespan>=2.1.0       # For ASGI lifespan events in tests
